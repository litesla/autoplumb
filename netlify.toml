[build]
  command = "npm run build"
  publish = "dist"

[functions."*"]
  external_node_modules = [
    "firebase-admin",
    "xlsx",
    "axios",
    "lightningcss"
  ]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
